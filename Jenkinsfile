node {
    stage('Checkout'){
        checkout scm
    }

    stage('lint') {
        println "nothing here"
    }

    stage('Test') {
        println "nothing here"
    }

    stage('Deploy') {
        if (env.BRANCH_NAME == 'master') {
            println "nothing here"
        }
    }
}
